import React from 'react';
import {
    SafeAreaView,
    StyleSheet,
    ScrollView,
    View,
    Text,
    StatusBar,
    PermissionsAndroid,
    Alert,
} from 'react-native';
import { Button } from 'galio-framework';

const Login = ({}) => {
    return (
        <View>
            <Text>
                Login
            </Text>
        </View>
    );
}

export default Login;